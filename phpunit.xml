<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="APP_URL" value="http://localhost"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="pgsql"/>
        <!-- DB_DATABASE is deliberately absent: tests/bootstrap.php derives one database
             per checkout, so the main clone and a git worktree cannot run migrate:fresh
             against the same schema. Set DB_DATABASE in the environment (as CI does) to
             override that. See Tests\Support\TestDatabase. -->
        <!-- The git fixtures are local bare repos cloned over file://. Production ships
             https,ssh; the tests that assert the scheme gate reset it to that default. -->
        <env name="KONTORFIX_VCS_ALLOWED_SCHEMES" value="https,ssh,file"/>
        <!-- Without this the suite falls through to config/logging.php's default and writes
             into the developer's own storage/logs/laravel.log, which nothing rotates. The
             `testing` channel is a rotated file of its own (storage/logs/testing-*.log, 3
             days) — kept rather than silenced so a failure whose cause is only visible in a
             log entry stays diagnosable. See config/logging.php. -->
        <env name="LOG_CHANNEL" value="testing"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
